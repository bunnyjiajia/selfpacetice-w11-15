// เมื่อหน้าถูกโหลดเสร็จ ให้รันฟังก์ชันนี้
document.addEventListener("DOMContentLoaded", () => {
  console.log("หน้าเว็บโหลดเสร็จแล้ว");

  // 1. เลือกปุ่มจาก HTML โดยใช้ id
  const btn = document.getElementById("clickBtn");

  // 2. เลือก <p> ไว้สำหรับเปลี่ยนข้อความ
  const msg = document.getElementById("message");

  // 3. เพิ่ม event ให้ปุ่ม เมื่อคลิกแล้วให้ทำอะไรสักอย่าง
  btn.addEventListener("click", () => {
    msg.textContent = "คุณกดปุ่มแล้ว! 🎉"; // เปลี่ยนข้อความ
    msg.style.color = "blue";               // เปลี่ยนสี
    console.log("ปุ่มถูกคลิก");            // log ใน console
  });
});
